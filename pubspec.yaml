name: uts_2020130036
description: Neko Shop E-Commers Apps
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ">=3.1.4 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.6
  flutter_rating_bar: ^4.0.1
  intl: ^0.18.0
  flutter_localizations:
      sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  google_fonts: ^6.1.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0
  flutter_native_splash: ^2.3.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  #Assets Images
  assets: 
    #Products
    - assets/images/Products/Product_01.png
    - assets/images/Products/Product_02.png
    - assets/images/Products/Product_03.png
    - assets/images/Products/Product_04.png
    - assets/images/Products/Product_05.png
    - assets/images/Products/Product_06.png

    #Profile
    - assets/profiles/M.Ariq_Rasyid.png
    - assets/profiles/img_person_1.png
    - assets/profiles/img_person_2.png
    - assets/profiles/img_person.png

    # Logo
    - assets/logo/piclogo.png

    # Splash Screen
    - assets/images/Splash Screens/Logo_Neko Shop_Splash Screen.png

    # ETC
    - assets/images/img_card.png
    - assets/images/img_check.png

  # Icons
    - assets/icons/icons/ic_add.svg
    - assets/icons/icons/ic_bag.svg
    - assets/icons/icons/ic_bell_fill.svg
    - assets/icons/icons/ic_work.svg
    - assets/icons/icons/ic_star.svg
    - assets/icons/icons/ic_popular.svg
    - assets/icons/icons/ic_plus.svg
    - assets/icons/icons/ic_person.svg
    - assets/icons/icons/ic_person_fill.svg
    - assets/icons/icons/ic_minus.svg
    - assets/icons/icons/ic_marker.svg
    - assets/icons/icons/ic_marker_fill.svg
    - assets/icons/icons/ic_home.svg
    - assets/icons/icons/ic_home_fill.svg
    - assets/icons/icons/ic_edit.svg
    - assets/icons/icons/ic_content_creator.svg
    - assets/icons/icons/ic_checkmark.svg
    - assets/icons/icons/ic_cart.svg
    - assets/icons/icons/ic_cart_2.svg
    - assets/icons/icons/ic_card.svg
    - assets/icons/icons/ic_bell.svg
    - assets/icons/icons/ic_game.svg
    - assets/icons/icons/ic_search.svg
    - assets/icons/icons/ic_cancel.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
  
  generate: true

  
  fonts:
    - family: Gelasio
      fonts:
        - asset: assets/fonts/gelasio/Gelasio-Regular.ttf
          weight: 400
        - asset: assets/fonts/gelasio/Gelasio-Medium.ttf
          weight: 500
        - asset: assets/fonts/gelasio/Gelasio-Bold.ttf
          weight: 700


    - family: NunitoSans
      fonts:
        - asset: assets/fonts/nunito_sans/NunitoSans_10pt-Bold.ttf
          weight: 700
        - asset: assets/fonts/nunito_sans/NunitoSans_10pt-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/nunito_sans/NunitoSans_10pt-Medium.ttf
          weight: 500
        - asset: assets/fonts/nunito_sans/NunitoSans_10pt-Regular.ttf
          weight: 400
        - asset: assets/fonts/nunito_sans/NunitoSans_10pt-Light.ttf
          weight: 300


    - family: MerriWeather
      fonts:
        - asset: assets/fonts/merriweather/Merriweather-Bold.ttf
          weight: 700
        - asset: assets/fonts/merriweather/Merriweather-Regular.ttf
          weight: 400
        - asset: assets/fonts/merriweather/Merriweather-Light.ttf
          weight: 300
flutter_native_splash:
  color: "#42a5f5"
  image: "assets/images/Splash Screens/Logo_Neko Shop_Splash Screen.png"